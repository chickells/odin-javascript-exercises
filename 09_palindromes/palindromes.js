const palindromes = function (string) {
    const stringToArray = '' // create function here (remove spaces - if character == " ", skip to next index in array)
    const reversed = stringToArray //.reverseFunction() start at back and subtract from array[array.length] until its array[0]
    return string === reversed // should return true or false..... *SHOULD* lol
};

// take string, split it per character
// make a new array that's reversed
// check if new array equals original array
// BOOM EASY

// Do not edit below this line
module.exports = palindromes;
